# .github/workflows/deploy.yml
name: 🚀 Deploy

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: [self-hosted, Linux, ARM64]
    
    # ✅ Add this to opt into Node.js 24 early:
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
      # ... rest of your steps
